import React from 'react';

const Register = () => {
    return (
        <div>
            {/* <form onSubmit={handleRegister}>
        <input onBlur={handleOnChangeBlurEmail} type="email" name="email" id="" placeholder='Your Email' required/>
        <br />
        <input onBlur={ handleOnChangeBlurPassword} type="password" name="password" id="" placeholder='Your Password' required />
        <br />
        <button type='submit'>Register</button>
      </form> */}
        </div>
    );
};

export default Register;